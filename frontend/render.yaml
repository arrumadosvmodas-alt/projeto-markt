services:
  - type: web
    name: markt-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /api/*
        destination: https://markt-backend.onrender.com/*
      - type: rewrite
        source: /*
        destination: /index.html
